<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, shrink-to-fit=no, user-scalable=no"/>
    <title>Assemblage</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header class="main__wrapper main__header">
      <h1 class="main__header__branding">Assemblage</h1>
      <section class="main__header__meta">
        <%= if @current_user do %>
          <p><%= @current_user.username %></p>
          <p><%= link "Log out", to: Routes.session_path(@conn, :delete, @current_user), method: "delete" %></p>
        <% else %>
          <p><%= link "Register", to: Routes.user_path(@conn, :new) %></p>
          <p><%= link "Log in", to: Routes.session_path(@conn, :new) %></p>
        <% end %>
      </section>
    </header>
    <main class="main__wrapper main__content">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <footer class="main__wrapper main__footer">
      <p>Some footer stuff</p>
    </footer>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
